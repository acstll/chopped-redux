{
  "name": "redux-mini",
  "version": "1.0.0",
  "dependencies": {
    "lodash.mapvalues": "^3.0.1"
  },
  "devDependencies": {
    "faucet": "0.0.1",
    "standard": "^4.3.2",
    "tape": "^4.0.0"
  },
  "scripts": {
    "test": "npm run lint && node test/index.js | faucet",
    "lint": "standard ."
  }
}
